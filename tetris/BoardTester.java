package se.liu.chrer268.tetris;

public class BoardTester
{
    public static void main(String[] args) {
	/**for (int i = 1; i <= 10; i++) {
	    Board createdBoard = new Board(8, 10);
	    createdBoard.createRandomBoard(8, 10);
	    BoardToTextConverter convertedBoard = new BoardToTextConverter();
	    System.out.println("Board " + i + ":" + "\n" + convertedBoard.convertToText(createdBoard));
	}*/

	/*Board createdBoard = new Board(15, 10);
	TetrisViewer newView = new TetrisViewer(createdBoard);
	newView.readKeyInput();
	newView.timer();*/
    }
}
